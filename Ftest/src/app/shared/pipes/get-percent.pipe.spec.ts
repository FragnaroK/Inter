import { GetPercentPipe } from './get-percent.pipe';

describe('GetPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
